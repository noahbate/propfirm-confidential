services:
  - type: web
    name: prop-firm-api
    runtime: python
    plan: free
    buildCommand: bash backend/build.sh
    startCommand: uvicorn backend.src.main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
