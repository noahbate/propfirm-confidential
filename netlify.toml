# Netlify Build & Deploy Configuration

[build]
  # Directory to change to before starting a build.
  base = "prop-firm-app"

  # Command to execute to build the project.
  command = "npm install && npm run build"

  # Directory to publish (relative to the base directory).
  publish = "dist"

[context.production]
  # Custom settings for the production environment
  # You can override any build settings here if needed

[context.staging]
  # Custom settings for a staging branch (e.g., 'develop')
  # command = "npm install && npm run build:staging"
